<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AE Game Store</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <header class="hero">
        <div class="hero-overlay"></div>
        <div class="hero-content">
            <h1>AE Game Store</h1>
            <p>Top Up & Digital Key Cepat, Aman, dan Otomatis</p>
        </div>
    </header>

    <main class="container">
        <section class="card store-card">
            <div class="left-panel">
                <h2>Beli Produk</h2>
                <p class="subtitle">Pilih game dan produk yang kamu mau, lalu lanjut bayar.</p>

                <label for="game">Pilih Game</label>
                <select id="game">
                    <option value="Mobile Legends">Mobile Legends</option>
                    <option value="Free Fire">Free Fire</option>
                    <option value="PUBG Mobile">PUBG Mobile</option>
                    <option value="Genshin Impact">Genshin Impact</option>
                    <option value="Game Key Premium">Game Key Premium</option>
                </select>

                <label for="product">Pilih Produk</label>
                <select id="product"></select>

                <label for="name">Nama Pembeli</label>
                <input type="text" id="name" placeholder="Masukkan nama kamu">

                <label for="contact">Kontak</label>
                <input type="text" id="contact" placeholder="WA / Telegram / Email">

                <button id="buyBtn" onclick="buy()">Beli Sekarang</button>
                <div class="loading" id="loading">Membuat invoice...</div>
            </div>

            <div class="right-panel">
                <div class="preview-box">
                    <span class="badge">Preview Order</span>
                    <h3 id="previewGame">Mobile Legends</h3>
                    <p id="previewProduct">86 Diamonds</p>
                    <div class="price-box">
                        <span>Total</span>
                        <strong id="previewPrice">Rp 20.000</strong>
                    </div>
                    <ul class="features">
                        <li>Proses otomatis</li>
                        <li>Pembayaran aman via Xendit</li>
                        <li>Key / produk dikirim setelah pembayaran sukses</li>
                    </ul>
                </div>
            </div>
        </section>
    </main>

    <script src="script.js"></script>
</body>
</html>

* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: #eef3f8;
    color: #1e293b;
}

.hero {
    position: relative;
    background: linear-gradient(135deg, #0f172a, #1d4ed8, #38bdf8);
    color: white;
    padding: 70px 20px 100px;
    text-align: center;
}

.hero-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.15);
}

.hero-content {
    position: relative;
    z-index: 1;
}

.hero h1 {
    margin: 0;
    font-size: 42px;
}

.hero p {
    margin-top: 12px;
    font-size: 16px;
    opacity: 0.95;
}

.container {
    max-width: 1100px;
    margin: -45px auto 40px;
    padding: 20px;
    position: relative;
    z-index: 2;
}

.store-card {
    background: white;
    border-radius: 18px;
    box-shadow: 0 14px 40px rgba(0, 0, 0, 0.12);
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 24px;
    padding: 28px;
}

.left-panel h2 {
    margin-top: 0;
    margin-bottom: 8px;
}

.subtitle {
    margin-top: 0;
    margin-bottom: 20px;
    color: #64748b;
}

label {
    display: block;
    margin-bottom: 8px;
    margin-top: 14px;
    font-weight: bold;
    font-size: 14px;
}

input,
select {
    width: 100%;
    padding: 13px 14px;
    border: 1px solid #cbd5e1;
    border-radius: 10px;
    font-size: 14px;
    background: #fff;
    outline: none;
}

input:focus,
select:focus {
    border-color: #3b82f6;
}

button {
    width: 100%;
    margin-top: 20px;
    padding: 14px;
    border: none;
    border-radius: 12px;
    background: linear-gradient(135deg, #2563eb, #38bdf8);
    color: white;
    font-size: 16px;
    font-weight: bold;
    cursor: pointer;
    transition: transform 0.15s ease, opacity 0.15s ease;
}

button:hover {
    transform: translateY(-1px);
    opacity: 0.96;
}

.loading {
    display: none;
    margin-top: 12px;
    text-align: center;
    color: #2563eb;
    font-size: 14px;
}

.preview-box {
    background: linear-gradient(180deg, #f8fbff, #eef6ff);
    border: 1px solid #dbeafe;
    border-radius: 16px;
    padding: 22px;
    height: 100%;
}

.badge {
    display: inline-block;
    background: #dbeafe;
    color: #1d4ed8;
    font-size: 12px;
    font-weight: bold;
    padding: 6px 10px;
    border-radius: 999px;
    margin-bottom: 12px;
}

.preview-box h3 {
    margin: 0 0 8px;
    font-size: 26px;
}

.preview-box p {
    margin: 0 0 20px;
    color: #475569;
}

.price-box {
    background: white;
    border-radius: 14px;
    padding: 16px;
    margin-bottom: 20px;
    border: 1px solid #dbeafe;
}

.price-box span {
    display: block;
    color: #64748b;
    margin-bottom: 6px;
}

.price-box strong {
    font-size: 28px;
    color: #2563eb;
}

.features {
    padding-left: 18px;
    margin: 0;
    color: #334155;
    line-height: 1.8;
}

@media (max-width: 900px) {
    .store-card {
        grid-template-columns: 1fr;
    }

    .hero h1 {
        font-size: 32px;
    }
}

const products = {
    "Mobile Legends": [
        { name: "86 Diamonds", price: 20000 },
        { name: "172 Diamonds", price: 38000 },
        { name: "257 Diamonds", price: 55000 }
    ],
    "Free Fire": [
        { name: "70 Diamonds", price: 10000 },
        { name: "140 Diamonds", price: 19000 },
        { name: "355 Diamonds", price: 47000 }
    ],
    "PUBG Mobile": [
        { name: "60 UC", price: 15000 },
        { name: "325 UC", price: 76000 },
        { name: "660 UC", price: 145000 }
    ],
    "Genshin Impact": [
        { name: "Blessing Welkin", price: 70000 },
        { name: "60 Genesis Crystals", price: 16000 },
        { name: "300 Genesis Crystals", price: 79000 }
    ],
    "Game Key Premium": [
        { name: "1 Key Premium", price: 50000 },
        { name: "2 Key Premium", price: 95000 },
        { name: "3 Key Premium", price: 140000 }
    ]
};

const gameSelect = document.getElementById("game");
const productSelect = document.getElementById("product");

function formatRupiah(num) {
    return "Rp " + num.toLocaleString("id-ID");
}

function loadProducts() {
    const selectedGame = gameSelect.value;
    const gameProducts = products[selectedGame];

    productSelect.innerHTML = "";

    gameProducts.forEach((item, index) => {
        const option = document.createElement("option");
        option.value = index;
        option.textContent = `${item.name} - ${formatRupiah(item.price)}`;
        productSelect.appendChild(option);
    });

    updatePreview();
}

function updatePreview() {
    const selectedGame = gameSelect.value;
    const selectedProduct = products[selectedGame][productSelect.value];

    document.getElementById("previewGame").innerText = selectedGame;
    document.getElementById("previewProduct").innerText = selectedProduct.name;
    document.getElementById("previewPrice").innerText = formatRupiah(selectedProduct.price);
}

gameSelect.addEventListener("change", loadProducts);
productSelect.addEventListener("change", updatePreview);

async function buy() {
    const name = document.getElementById("name").value.trim();
    const contact = document.getElementById("contact").value.trim();
    const selectedGame = gameSelect.value;
    const selectedProduct = products[selectedGame][productSelect.value];

    if (!name || !contact) {
        alert("Isi nama dan kontak dulu!");
        return;
    }

    document.getElementById("loading").style.display = "block";
    document.getElementById("buyBtn").disabled = true;

    try {
        const res = await fetch("/create-order", {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({
                name,
                contact,
                game: selectedGame,
                product: selectedProduct.name,
                price: selectedProduct.price
            })
        });

        const data = await res.json();

        if (data.invoiceUrl) {
            window.location.href = data.invoiceUrl;
        } else {
            alert(data.message || "Gagal membuat invoice");
        }
    } catch (err) {
        alert("Terjadi error server");
    }

    document.getElementById("loading").style.display = "none";
    document.getElementById("buyBtn").disabled = false;
}

loadProducts();

const db = require("./database");
const express = require("express");
const path = require("path");
require("dotenv").config();

const app = express();
const port = process.env.PORT || 3000;

app.use(express.json());
app.use(express.static("public"));

app.get("/", (req, res) => {
    res.sendFile(path.join(__dirname, "public", "index.html"));
});

app.get("/result", (req, res) => {
    res.sendFile(path.join(__dirname, "public", "result.html"));
});

// buat order + invoice Xendit
app.post("/create-order", async (req, res) => {
    const { name, contact, game, product, price } = req.body;

    const orderId = "ORDER-" + Date.now();

    const newOrder = {
        id: orderId,
        name: name,
        contact: contact,
        game: game,
        product: product,
        price: price,
        status: "pending"
    };

    db.run(
        "INSERT INTO orders (id, name, contact, status) VALUES (?, ?, ?, ?)",
        [orderId, name, contact, "pending"],
        async (err) => {
            if (err) {
                console.error("ERROR INSERT ORDER:", err);
                return res.status(500).json({
                    message: "Gagal menyimpan order ke database"
                });
            }

            console.log("Order baru:", newOrder);

            try {
                const auth = Buffer.from(process.env.XENDIT_SECRET_KEY + ":").toString("base64");

                const xenditResponse = await fetch("https://api.xendit.co/v2/invoices", {
                    method: "POST",
                    headers: {
                        "Content-Type": "application/json",
                        "Authorization": "Basic " + auth
                    },
                    body: JSON.stringify({
                        external_id: orderId,
                        amount: price,
                        payer_email: "test@example.com",
                        description: `Pembayaran untuk ${game} - ${product}`,
                        success_redirect_url: "http://localhost:3000/result?order_id=" + orderId,
                        failure_redirect_url: "http://localhost:3000/result?order_id=" + orderId
                    })
                });

                const rawText = await xenditResponse.text();
                console.log("STATUS XENDIT:", xenditResponse.status);
                console.log("RESPON XENDIT RAW:", rawText);

                let data;
                try {
                    data = JSON.parse(rawText);
                } catch (error) {
                    return res.status(500).json({
                        message: "Response Xendit bukan JSON"
                    });
                }

                if (!xenditResponse.ok) {
                    return res.status(500).json({
                        message: data.message || "Gagal membuat invoice di Xendit"
                    });
                }

                if (!data.invoice_url) {
                    return res.status(500).json({
                        message: "invoice_url tidak ada dari Xendit",
                        xenditData: data
                    });
                }

                res.json({
                    message: "Invoice berhasil dibuat!",
                    invoiceUrl: data.invoice_url
                });

            } catch (error) {
                console.error("ERROR SERVER:", error);

                res.status(500).json({
                    message: "Server gagal menghubungi Xendit"
                });
            }
        }
    );
});

app.post("/xendit-webhook", (req, res) => {
    try {
        console.log("=== WEBHOOK MASUK ===");
        console.log(req.body);

        const callbackToken = req.headers["x-callback-token"];

        if (callbackToken !== process.env.XENDIT_CALLBACK_TOKEN) {
            console.log("TOKEN SALAH!");
            return res.status(403).send("Forbidden");
        }

        const data = req.body;

        console.log("STATUS:", data.status);
        console.log("ORDER ID:", data.external_id);

        if (data.status === "PAID") {
            const orderId = data.external_id;

            db.get("SELECT * FROM orders WHERE id = ?", [orderId], (err, order) => {
                if (err) {
                    console.log("ERROR CARI ORDER:", err);
                    return;
                }

                if (!order) {
                    console.log("ORDER TIDAK DITEMUKAN:", orderId);
                    return;
                }

                if (order.status === "paid" && order.gameKey) {
                    console.log("ORDER SUDAH PERNAH DIPROSES:", orderId);
                    return;
                }

                // ambil 1 key yang belum dipakai
                db.get("SELECT * FROM keys WHERE used = 0 LIMIT 1", (err, keyRow) => {
                    if (err) {
                        console.log("ERROR AMBIL KEY:", err);
                        return;
                    }

                    let key = "STOK HABIS";

                    if (keyRow) {
                        key = keyRow.key;

                        // tandai key sebagai sudah dipakai
                        db.run("UPDATE keys SET used = 1 WHERE id = ?", [keyRow.id], (err) => {
                            if (err) {
                                console.log("ERROR UPDATE KEY:", err);
                            }
                        });
                    }

                    // update order
                    db.run(
                        "UPDATE orders SET status = ?, gameKey = ? WHERE id = ?",
                        ["paid", key, orderId],
                        (err) => {
                            if (err) {
                                console.log("ERROR UPDATE ORDER:", err);
                                return;
                            }

                            console.log("ORDER SUDAH DIBAYAR:", orderId);
                            console.log("KEY TERKIRIM:", key);
                        }
                    );
                });
            });
        }

        res.status(200).send("OK");
    } catch (err) {
        console.log("ERROR WEBHOOK:", err);
        res.status(500).send("ERROR");
    }
});

app.get("/order/:id", (req, res) => {
    const orderId = req.params.id;

    db.get("SELECT * FROM orders WHERE id = ?", [orderId], (err, order) => {
        if (err) {
            return res.status(500).send("Gagal mengambil data order");
        }

        if (!order) {
            return res.status(404).send("Order tidak ditemukan");
        }

        res.json(order);
    });
});

app.listen(port, () => {
    console.log(`Server jalan di http://localhost:${port}`);
});

const sqlite3 = require("sqlite3").verbose();

const db = new sqlite3.Database("./database.db");

db.serialize(() => {
    db.run(`
        CREATE TABLE IF NOT EXISTS orders (
            id TEXT PRIMARY KEY,
            name TEXT,
            contact TEXT,
            status TEXT,
            gameKey TEXT
        )
    `);

    db.run(`
        CREATE TABLE IF NOT EXISTS keys (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            key TEXT,
            used INTEGER DEFAULT 0
        )
    `);
});

module.exports = db;

<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hasil Pembayaran</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #eef7ff;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
        }

        .card {
            background: white;
            padding: 30px;
            border-radius: 16px;
            box-shadow: 0 6px 18px rgba(0,0,0,0.12);
            width: 100%;
            max-width: 460px;
            text-align: left;
        }

        h1 {
            margin-top: 0;
            color: #1f2d3d;
            text-align: center;
        }

        p {
            color: #444;
            margin: 10px 0;
        }

        .key-box {
            margin-top: 20px;
            padding: 14px;
            background: #f4faff;
            border: 1px solid #b9ddff;
            border-radius: 10px;
            font-weight: bold;
            color: #0070cc;
            word-break: break-word;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1>Hasil Pembayaran</h1>
        <p id="statusText">Memuat data...</p>
        <p id="nameText"></p>
        <p id="contactText"></p>
        <p id="gameText"></p>
        <p id="productText"></p>
        <p id="priceText"></p>
        <div class="key-box" id="keyBox">Menunggu key...</div>
    </div>

    <script>
        function formatRupiah(num) {
            return "Rp " + Number(num).toLocaleString("id-ID");
        }

        async function loadOrder() {
            const params = new URLSearchParams(window.location.search);
            const orderId = params.get("order_id");

            if (!orderId) {
                document.getElementById("statusText").innerText = "Order ID tidak ditemukan.";
                return;
            }

            try {
                const response = await fetch("/order/" + orderId);
                const data = await response.json();

                document.getElementById("statusText").innerText = "Status: " + (data.status || "-");
                document.getElementById("nameText").innerText = "Nama: " + (data.name || "-");
                document.getElementById("contactText").innerText = "Kontak: " + (data.contact || "-");
                document.getElementById("gameText").innerText = "Game: " + (data.game || "-");
                document.getElementById("productText").innerText = "Produk: " + (data.product || "-");
                document.getElementById("priceText").innerText = "Harga: " + (data.price ? formatRupiah(data.price) : "-");
                document.getElementById("keyBox").innerText = "Game Key: " + (data.gameKey || "Belum tersedia");
            } catch (error) {
                document.getElementById("statusText").innerText = "Gagal mengambil data order.";
            }
        }

        loadOrder();
    </script>
</body>
</html>