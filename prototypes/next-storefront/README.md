# Next Storefront Prototype

Prototype Next.js untuk eksperimen UI AE Game Store. Aplikasi production tetap
berada di root repository dan disajikan Express dari folder `public/`.

```bash
npm install
npm run dev
```

Buka `http://localhost:3000`. Konfigurasi lokal prototype disimpan di
`.env.local` dan tidak boleh di-commit.
